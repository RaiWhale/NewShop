@{
    ViewBag.Title = "Index";
}
@model TechnologyShop.Areas.Admin.Models.FilesViewModel
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<!-- blueimp File Upload & Gallery styles -->
@Styles.Render("~/Content/Admin/jQuery-File-Upload")
@*<script>

        $(function () {
            $('#fileUpload').fileupload({
                dataType: 'json',
                done: function (e, data) {
                    $.each(data.result.files, function (index, file) {
                        $('<p/>').text(file.name).appendTo(document.body);
                    });
                }
            });
        });
    </script>*@
<script>
    var $form = null;
    $(function () {
        $form = $('#fileupload').fileupload({
            dataType: 'json'
        });

    });
    $('#fileupload').addClass('fileupload-processing');

</script>
<div class="well">
    @Html.Partial("_Files", Model)
</div>

@Scripts.Render("~/bundles/Admin/jQuery-File-Upload")

